import React, { ReactElement } from 'react';

interface Props {}

export default function addProduct({}: Props): ReactElement {
    return (
        <div>
            <h1>添加商品</h1>
        </div>
    );
}
